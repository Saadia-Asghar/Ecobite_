# EcoBite Server Configuration - DEMO MODE
# Copy this file to server/.env to run the prototype

# Server Configuration
PORT=3002
NODE_ENV=development

# JWT Secret
JWT_SECRET=ecobite_demo_secret_key_2024

# ===================================
# EMAIL CONFIGURATION
# ===================================

# Choose ONE of the following email providers:

# Option 1: Gmail (For Testing)
# 1. Enable 2FA on your Gmail account
# 2. Generate App Password: https://myaccount.google.com/apppasswords
# 3. Use the 16-character app password below
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-16-char-app-password

# Option 2: SendGrid (Recommended for Production)
# 1. Sign up at https://sendgrid.com/
# 2. Create API Key with Mail Send permission
# 3. Use 'apikey' as SMTP_USER and your API key as SMTP_PASSWORD
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=apikey
# SMTP_PASSWORD=your-sendgrid-api-key

# Option 3: AWS SES (Best for Scale)
# 1. Create AWS account and verify email
# 2. Get SMTP credentials from SES Console
# SMTP_HOST=email-smtp.us-east-1.amazonaws.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-aws-smtp-username
# SMTP_PASSWORD=your-aws-smtp-password

# Application URLs (for email links)
FRONTEND_URL=http://localhost:5173
BACKEND_URL=http://localhost:3002

# ===================================
# PAYMENT GATEWAYS
# ===================================

# Stripe - DEMO MODE (No account needed!)
STRIPE_SECRET_KEY=sk_test_demo_mode
STRIPE_PUBLISHABLE_KEY=pk_test_demo_mode
STRIPE_WEBHOOK_SECRET=whsec_demo_mode

# JazzCash - DEMO MODE (No account needed!)
JAZZCASH_MERCHANT_ID=MC_DEMO_12345
JAZZCASH_PASSWORD=demo_password
JAZZCASH_INTEGRITY_SALT=demo_salt_key_123
JAZZCASH_RETURN_URL=http://localhost:5173/payment/jazzcash/return
JAZZCASH_API_URL=http://localhost:3002/api/payment/jazzcash/mock

# ===================================
# DATABASE (PostgreSQL)
# ===================================

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=ecobite
DB_USER=postgres
DB_PASSWORD=your_password_here

# ===================================
# NOTES
# ===================================

# Email Notifications:
# - Welcome emails on registration
# - Password reset emails
# - Payment verification emails
# - Money request approval/rejection emails

# Payment Gateways:
# - Stripe: For card payments (demo mode)
# - JazzCash: For mobile wallet payments (demo mode)

# To enable real email notifications:
# 1. Choose an email provider above
# 2. Uncomment and configure the SMTP settings
# 3. Restart the server
